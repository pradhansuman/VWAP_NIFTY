# Upstox developer app — https://account.upstox.com/developer/apps
UPSTOX_API_KEY=
UPSTOX_API_SECRET=

# Daily access token or analytics token. Also pasteable on /connect
UPSTOX_ACCESS_TOKEN=

# Must match the redirect URI registered on the Upstox app
UPSTOX_REDIRECT_URI=http://127.0.0.1:43127/api/upstox/callback
